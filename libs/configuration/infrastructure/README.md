# configuration-infrastructure

Infrastructure library for configuration adapters.

This library provides concrete configuration models and adapters used by the configuration domain.

## Purpose

- Define application configuration data models.
- Provide infrastructure-specific wiring for configuration values.

## Structure

- `src/lib/models.ts`: configuration model definitions.

## Commands

- `nx lint configuration-infrastructure`
